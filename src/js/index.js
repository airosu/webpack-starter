import { add1 } from './utils/test_intFunctions';
import { add2 } from './utils/test_intFunctions';
import { add3 } from './test_app';

const one = add1( 0 );
const two = add2( 0 );
const three = add3( 0 );

console.log( 'Index 1 numbers: ', one, two, three );

